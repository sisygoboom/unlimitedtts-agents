# @unlimitedtts/mcp

Stdio MCP server for quote-first, external-wallet x402 text-to-speech. See the repository README for configuration and security behavior.

```bash
npx -y @unlimitedtts/mcp@0.1.0
```

