{
  "name": "unlimitedtts-agents",
  "version": "0.1.0",
  "private": true,
  "description": "UnlimitedTTS MCP server, agent skill, and OpenClaw integration",
  "repository": {
    "type": "git",
    "url": "https://github.com/sisygoboom/unlimitedtts-agents.git"
  },
  "license": "MIT",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm --recursive --if-present run build",
    "check": "pnpm run build && pnpm run test",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "pnpm --recursive --if-present run clean"
  },
  "devDependencies": {
    "@types/node": "26.1.2",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
